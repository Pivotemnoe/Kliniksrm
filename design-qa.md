# Design QA — компактная навигация личного кабинета

## Артефакты и состояние

- Источник визуальной правды: `/Users/konstantin/Downloads/IMG_3431.PNG`.
- Реализация: `/private/tmp/owner-cabinet-compact-nav-430x932-v2.png`.
- Полное сравнение: `/private/tmp/owner-cabinet-nav-comparison.png`.
- Фокусное сравнение навигации: `/private/tmp/owner-cabinet-nav-focus.png`.
- Состояние: авторизованный владелец, 2 пациента, 1 запись, 1 сообщение; открыт раздел «Пациенты».
- CSS viewport реализации: `430 × 932`, `deviceScaleFactor: 1`.
- Исходный снимок: `1290 × 2796`, то есть точный `@3x` для `430 × 932`; перед сравнением уменьшен до `430 × 932`.
- Снимок реализации: `430 × 932`; дополнительная проверка выполнена на `320 × 800`.
- Различия состояния, не относящиеся к задаче: в источнике присутствует системная строка iPhone и разрешённые уведомления, в локальном браузерном рендере системной строки нет и уведомления запрещены.

## Проверенные поверхности

- Шрифты и типографика: сохранены текущий системный стек, веса и иерархия; подписи разделов читаются без обрезания.
- Ритм и компоновка: горизонтальная лента заменена единой карточкой; на ширине 430 px первые шесть разделов занимают сетку `3 × 2`, «Сообщения» — полную третью строку. На ширине 320 px сетка безопасно переключается на две колонки.
- Цвета и токены: сохранены существующие тёмно-синий, бирюзовый и светло-серый состояния; активный раздел остаётся заметным.
- Изображения: используется прежний логотип, новые или заменяющие изображения не добавлялись.
- Текст: сохранены все семь разделов и их счётчики; добавлен только заголовок «Разделы кабинета».

## Полное и фокусное сравнение

- Полный вид подтверждает, что шапка, карточка владельца и сервисный блок визуально не изменены; целевое изменение ограничено навигацией.
- Фокусный вид показывает исходную обрезанную горизонтальную ленту рядом с новой полностью видимой сеткой. Подписи и активное состояние читаются на масштабе 1:1.

## История итераций

1. Первый рендер: `/private/tmp/owner-cabinet-compact-nav-430x932.png`.
   - [P2] Сетка из двух колонок убрала горизонтальную прокрутку, но четвёртая строка не помещалась в исходный видимый участок и была недостаточно компактной.
   - Исправление: для экранов 361–700 px включена сетка из трёх колонок; «Сообщения» оставлены отдельной полной строкой. Для экранов до 360 px сохранены две колонки с крупными зонами нажатия.
2. Повторный рендер: `/private/tmp/owner-cabinet-compact-nav-430x932-v2.png`.
   - Все семь разделов одновременно видны внутри одной карточки; горизонтального переполнения нет.
   - На 320 px: ширина сетки и `scrollWidth` равны 272 px, ширина документа равна viewport 320 px; кнопки имеют высоту 48 px.

## Функциональная проверка

- Переход в «Записи» показывает панель «Записи в клинику».
- Переход в «Сообщения» показывает панель «Сообщения клиники».
- В консоли браузера нет предупреждений и ошибок.
- Автоматические проверки: 191 из 191 пройдены.

## Findings

Нет оставшихся P0, P1 или P2 расхождений. Целевая проблема — скрытые разделы и необходимость горизонтального жеста — устранена.

## Follow-up polish

Оставшихся обязательных визуальных доработок нет.

final result: passed

---

# Design QA — склад и таблица лабораторных результатов

Дата проверки: 22.08.2026.

## Источник и реализация

- Исходный экран: `/Users/konstantin/Desktop/download.png`.
- Реализация: локальная сборка TemichevVet CRM с тестовыми данными, без подключения к рабочей CRM и базе.
- Сравнение исходного и исправленного экранов: `/private/tmp/temichevvet-stock-source-vs-after.png`.

## Проверки

- На ширине 1366 px таблица товаров имеет постоянную верхнюю полосу прокрутки высотой 20 px: видимая область 1170 px, ширина содержимого 1560 px.
- Название и категория ограничены по ширине и показывают полное значение во всплывающей подсказке; остаток, минимальный остаток и единицы не переносятся хаотично.
- Колонки количества видны без возврата к началу таблицы; колонка действий остаётся закреплённой справа.
- На ширине 1920 px в одном экране видны все основные колонки от названия до НДС и действий.
- В таблице лаборатории проверено 120 строк. Ввод 20 цифр проходит без обновления массива таблицы на каждое нажатие; значение сохраняется после потери фокуса, статус строки меняется с «Назначен» на «Готов».

## Результат

PASSED — обе заявленные проблемы исправлены и подтверждены локальной браузерной проверкой.
