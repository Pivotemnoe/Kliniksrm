# Nginx

Nginx configuration will live here.

Expected responsibilities:

- TLS termination in production;
- reverse proxy to the API;
- serving the built web app;
- upload size limits;
- security headers;
- access logs.

