# Shared Package

Shared schemas, DTO types, constants and validation helpers will live here.

This package should contain code that is genuinely shared by `apps/web` and `apps/api`, especially Zod schemas and API contract types.

