# Публикация сайта TemichevVET 24/7 — 5 сентября 2026

## Результат

Боевой адрес: https://clinic.temichevvet.ru/ . Опубликованы 16 страниц выбранного направления 24/7, существующие реальные фотографии, публичный прайс CRM и официальный виджет отзывов Яндекса.

Пользователь разрешил публикацию сайта и интеграцию, затем уточнил: показывать действующие клинические услуги, кроме внутренних/служебных; позиции с ценой 0 ₽ пока не публиковать. После проверки 334 действующих позиций отмечены ровно 272. Остальные 62 не опубликованы: нулевые цены, тестовая категория, служебные позиции; груминг не относится к этому клиническому прайсу. Сами цены, названия, категории и история клинических записей не изменялись. Возможные дубли названий не удалялись и не объединялись.

В пяти незавершённых фотосекциях (специалисты, кабинеты приёма, рентген-кабинет, операционная/оборудование, ветаптека) опубликована надпись «РАЗДЕЛ В РАЗРАБОТКЕ» с пояснением, что клиника работает в обычном режиме. Технические подписи «здесь будут фотографии» не опубликованы. `/photo-plan` закрыт HTTP 404 и не открывается клиентским роутером production-сборки.

## Размещение и версии

- Российский сервер `193.188.23.65`, hostname `msk-1-vm-d817`: сайт и owner-gateway.
- `5.129.239.104`: только защищённый мост доступа. Файлы сайта и клиническая база на нём не размещались.
- TECNO `WIN-I123AM83GR4`, `C:\Users\TemichevVet\TemichevVet`: рабочая CRM и её база.
- API, CRM Web, owner-gateway: `b0e182fe3e2a1fe0461e920a27d5f2d50311bdc0`; GitHub сборка `33982588985` успешна.
- Каталог и основной сайт: исходный коммит `38801ea`; следующий `b0e182f` исключает нулевые цены при каждом экспорте.
- Финальная статическая сборка с маркировкой незавершённых разделов: `/var/www/temichevvet-clinic/releases/20260905-24-7-live-v2`, указатель `current`.
- SHA-256 финального архива: `8c7b7a89e0c7487aa7ecb6c9bb8627f68f5b08e26c28e18a42c529b954b3c577`.

## Резервные копии и изменения серверов

- RU: `/opt/temichevvet-owner-gateway/backups/clinic-site-20260905-38801ea/`. Сохранены предыдущие файлы сайта, ссылка на старую папку, Nginx, настройки gateway, сведения о контейнерах и `gateway.dump` (1 365 624 байта). SHA-256 dump: `5f47b27f867a525d670f400283fa67c5de769d1f01bd516813891b2c3acb069e`. Проверен список объектов через `pg_restore -l`.
- TECNO: `backups\manual-releases\clinic-site-20260905-b0e182f\database.dump` (16 533 475 байт), SHA-256 `e0f2d35b373bac16815be531a406d5956336623b482ba89858d15d238217ef53`. Сохранены предыдущие `.env`, Compose и сведения о контейнерах; проверен список объектов дампа. Полный тест восстановления в этом релизе не выполнялся.
- Применены две добавочные миграции `20260905130000_public_service_catalog`: флаг услуги в CRM и отдельная таблица публичного снимка на gateway. Для шлюза транзакционный SQL зарегистрирован в `_prisma_migrations` с SHA исходной миграции `0394ae5b6aa2fc9c383f90c4ecd148f05f298b600fec94214cbfc20f4b17a391`.
- На TECNO заменены только API и Web. Идентификаторы и время запуска PostgreSQL, Redis, MinIO и backup не изменились.
- На RU заменён только gateway; его предыдущий контейнер не имел mounts. Compose 1.29 не смог пересоздать контейнер из-за `ContainerConfig`; предыдущая версия была восстановлена, затем заменена только оболочка приложения. Старый образ и конфигурация сохранены. PostgreSQL и backup gateway не перезапускались.
- Windows credential helper не работал в SSH-сеансе. Публичные образы загружены с отдельной конфигурацией Docker без доступа к хранилищу паролей, платформа `amd64` и revision проверены. Системная политика Windows не изменена.
- Nginx проверен до reload. Изменён только vhost клиники: статические страницы, реальные 404, публичный GET каталога, CSP с официальным iframe Яндекса. DNS и соседние сервисы не менялись.

## Приёмка

- Финальный повторный прогон: 287/287 тестов CRM, включая исключение нулевых цен; API/gateway build и CRM web typecheck успешны. Сайт после финального изменения: 8/8 тестов.
- Внешний HTTPS: 16 страниц возвращают 200, уникальные title и canonical, индексация разрешена; неизвестный адрес, `/photo-plan`, внутренний API на домене сайта и отсутствующий asset возвращают 404.
- Публичный каталог: 272 позиции. Название, категория и суммы каждой сверены с разрешённым снимком действующей CRM. Нулевых цен и внутренних полей нет. `Cache-Control: no-store`; POST запрещён 403, запись на внутренний endpoint без секрета запрещена 403.
- В браузере проверены все 16 страниц при ширине 390 и 1280 px: один H1, нет горизонтального overflow и сломанных загруженных изображений. Поиск «Первичный» показывает две реальные позиции по 575 ₽. Дубли сохранены как отдельные услуги CRM.
- Реальный виджет Яндекса загрузился на production: карточка «Айболит», организация 1809394242. Наблюдались 4,8 / 354 отзыва / 805 оценок; эти значения не зафиксированы в интерфейсе.
- Периодический обмен идёт из рабочей CRM: timestamp снимка изменился с `2026-09-05T18:16:02.959Z` на `2026-09-05T18:19:02.957Z`, сохранилось 272 позиции. Фактические API/Web образы на TECNO подтверждены повторным postflight, экспорт включён. Рабочие финансовые цены специально для теста не редактировались. Изменение суммы и снятие услуги проверены ранее на синтетическом стенде, а не путём временного искажения боевого прайса.
- Доказательства: `outputs/clinic-site-release-20260905/` — HTTP-протокол, mobile/desktop checks, снимок выбранных услуг, SHA-проверенный SQL списка, postflight и скриншоты.

## Открытые пункты

1. Форма заявки намеренно не включена: не найден согласованный документ обработки персональных данных. На боевой странице вместо демонстрационной формы — звонок администратору. Получение заявки в рабочей CRM этим релизом не заявляется проверенным.
2. Метрика 112001598 и сбор аналитики из старого сайта не перенесены в новый интерфейс. Нужен отдельный согласованный сценарий согласия и проверка целей; публичный gateway endpoint аналитики сохранён, но новая страница его не вызывает.
3. Заполнить помеченные незавершённые разделы реальными материалами. Не добавлять вымышленные фотографии и квалификации.
4. Физические Safari/iPhone/Android не проверялись; выполнена браузерная приёмка мобильного viewport.

## Откат

Для сайта вернуть `current` на сохранённую предыдущую папку и при необходимости прежний vhost с `nginx -t` до reload. Для приложений вернуть конкретные прежние образы из release backup и пересоздать только соответствующий application container. Добавочные поле и таблицу не удалять. Отключение экспортёра само по себе не снимает уже опубликованный снимок: для немедленного скрытия закрыть публичный GET либо передать новый пустой снимок с канонического источника. Не удалять volumes, дампы и историю.
