# CRM interface and reliability release — 2026-09-24

Preserves the familiar left patient pane and existing autosave. Primary visits still require examination text and a diagnosis before completion; partial examination saves remain possible.

Changes:
- Count all low-stock/expiring items independently of the six-row dashboard preview.
- Compact operational notices; exclude billing and inventory notices for doctors who are not directors. Group vaccine reminders by animal, preserving owner and vaccine details.
- Wrap schedule names, pin queue actions, consolidate hospital columns, improve laboratory result editing and modal scrolling.
- Retry interrupted examination saves, flush pending drafts before completion, expose cancellation of completed visits under existing server permissions, avoid duplicate identical weights within one visit.
- Count distinct visits when learning medical phrases; an auxiliary learning failure does not fail the saved examination.
- Make box-sheet prescriptions optional and off by default. Exclude internal hospital notes from the standard owner report, improve print typography and PDF table continuation.
- Update embedded help and preserve a compact contextual assistant.

Validation before release: 408 unit tests passed; API build, Prisma client generation, web typecheck and production web build passed. Browser checks used actual React components with synthetic API fixtures, including interrupted-save recovery and both box-sheet modes. Server PDF continuation was rendered and inspected. Real clinical acceptance and physical printer output remain separate checks.

Deployment: TECNO CRM API and web only. Back up PostgreSQL and release configuration; rehearse additive migration `20260924000100_distinct_phrase_learning` against a restored isolated database; pin both images to the release commit; run Prisma migrate deploy, then `docker compose up -d --no-deps api web`. Verify health, exact image revisions and unchanged PostgreSQL/Redis/MinIO/backup container identities. Restore previous image pins for rollback, retaining the compatible additive schema. Never run seed or reset the clinical database.

Help maintenance: update the corresponding HelpPage section and date in the same change whenever labels, paths or print behavior change. There is no automatic synchronization of prose with the application.
